const PAGES_MANIFEST_SCRIPT_ID = "pages-manifest-script";

const FILE_TYPES = ["page", "layout", "error", "loader", "loading"] as const;

const JS_EXTS = [".js", ".ts", ".jsx", ".tsx"] as const;

export { FILE_TYPES, JS_EXTS, PAGES_MANIFEST_SCRIPT_ID };
